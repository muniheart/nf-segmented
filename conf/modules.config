env.APPTAINER_MESSAGELEVEL=5
env.SINGULARITY_MESSAGELEVEL=5

process {
    withName: NFCORE_DEMO {
        publishDir = [
            [
                path: { "${params.outdir}/${task.process.tokenize(':')[-1].toLowerCase()}" },
                pattern: '*.sqfs',
                mode: 'link',
            ]
        ]

        containerOptions = {
            [
                container_opts,
                "--env NXF_CACHE_DIR=/nextflow",
                "-B $child_outdir/.nextflow:/nextflow",
            ].join(' ')
        }
    }
}
